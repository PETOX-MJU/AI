# 라이선스 대장

상용 배포를 전제로 제품 경로에 들어가는 모든 구성요소를 추적한다.

## 확인 원칙

1. **보도자료가 아니라 모델 카드·데이터셋 페이지를 직접 연다.**
   카카오 Kanana 보도자료에는 "Apache 2.0"이라고 나오지만, VLM 변형 카드에는
   `License: kanana` 라고 적혀 있고 상업 조건이 없다. 같은 제품군 안에서도 변형마다 다르다.
2. **크기마다 다르다.** Qwen2.5-VL-7B 는 `apache-2.0` 이 명시돼 있지만 3B 카드에는 표기가 없다.
3. **라이선스는 바뀐다.** Qwen 은 2026년 8월까지 오픈웨이트 전부 Apache 2.0 이었으나 지금은 갈렸다.
4. **배포 직전에 이 표를 다시 검증한다.** 확인일을 반드시 기록한다.

## 제품 경로 (앱에 실제로 들어가는 것)

| 구성요소 | 라이선스 | 상용 | 확인일 | 비고 |
|---|---|---|---|---|
| ML Kit Subject Segmentation | Google 약관 | ✅ | 2026-09-04 | 온디바이스, 무료 |
| ML Kit Image Labeling | Google 약관 | ✅ | 2026-09-04 | Dog/Cat 라벨 내장, 학습 불필요 |
| MobileNetV3 ImageNet 가중치 | Apache 2.0 (가중치) | ⚠️ | 2026-09-04 | 아래 「회색지대」 참고 |
| TensorFlow / LiteRT | Apache 2.0 | ✅ | 2026-09-04 | |
| 숏폼 판별 학습 데이터 | 자체 제작 | ✅ | — | 아래 「데이터 전략」 참고 |
| ~~반려동물 분류 학습 데이터~~ | — | ✅ | — | **불필요** — ML Kit 이 유효성 판별을 담당 |

## 제품 경로 밖 (평가·비교 전용, 배포 금지)

| 구성요소 | 라이선스 | 위치 |
|---|---|---|
| VARCO-VISION 2.0 | CC-BY-NC-4.0 (비상업) | `experiments/shorts_detection/` |
| Qwen2.5-VL-7B-Instruct | Apache 2.0 | `experiments/pet_analysis_vlm/` |
| rembg / U2-Net | MIT / Apache 2.0 | `pixelart/` 프로토타입 |

`experiments/` 의 모델은 **제품에 탑재하지 않는다.** 성능 비교 기준선일 뿐이다.

## 검토했으나 채택하지 않은 것

| 모델 | 라이선스 | 불채택 사유 |
|---|---|---|
| Kanana 1.5-v-3b (카카오) | `kanana` 자체 | 상업 조건 불명. 쓰려면 카카오 문의 (kanana-mllm@kakaocorp.com) |
| Kanana-2 (카카오) | Kanana License | 4종 전부 **텍스트 전용** — 비전 변형 없음 |
| Qwen2.5-VL-3B | 카드에 표기 없음 | 확인 불가 |
| HyperCLOVA X SEED Vision | 자체 라이선스 | 상업 허용이나 조건 확인 필요 |
| SmolVLM2-500M | Apache 2.0 | 라이선스는 깨끗하나 품종 판별 정확도 부족 (아래) |

### SmolVLM2 실측 (온디바이스 VLM 후보였던 것)

- `litert-community/SmolVLM2-500M`, `.litertlm` 형식, 361MB
- Galaxy S26 GPU 백엔드 피크 메모리 404MB
- Apache 2.0

**라이선스는 문제없으나 500M VLM 은 세밀한 품종 판별을 못 한다.** "개"까지는 맞히지만
"웰시코기 vs 시바견"은 기대할 수 없다. 전용 분류기 조합이 30MB 미만으로 더 정확하다.
온디바이스 VLM 이 다시 필요해지면 이 항목부터 재검토하라.

## 데이터 전략 — 상용 배포의 핵심

**TDM(정보분석) 면책 조항은 국내에서 아직 입법되지 않았다.** 타사 앱 화면을 긁어
학습시키면 공정이용 판단에 기대야 하고, 상용 서비스에는 안전장치가 없다.

### 해법: 우리가 만든 영상을 우리가 촬영한다

1. 팀이 직접 제작한 짧은 영상을 유튜브 Shorts·인스타 릴스·틱톡에 올린다
2. 그 영상을 재생한 화면을 촬영해 `shorts` 라벨로 쓴다
3. `not_shorts` 도 팀 소유 화면·자체 콘텐츠로 채운다

**분류기가 학습하는 건 영상 내용이 아니라 UI 배치다** — 오른쪽 세로 버튼 열, 하단 캡션 바,
전체화면 세로 레이아웃. 그 신호는 어떤 영상을 틀든 동일하므로, 자체 콘텐츠로 바꿔도
정확도 손실이 없다. 저작권 쟁점만 사라진다.

품은 들지만 상용 배포를 목표로 한다면 이게 유일하게 깨끗한 경로다.

## 회색지대 (자문 항목)

**ImageNet 사전학습 가중치.** MobileNet·EfficientNet 의 ImageNet 가중치는 상업 앱에서
광범위하게 쓰이지만, ImageNet 원본 데이터셋 이용 약관은 연구 목적으로 한정된다.
가중치 자체는 Apache 2.0 으로 배포되나 파생 관계가 완전히 정리된 영역은 아니다.

업계 관행상 수용되는 위험이지만, **상용화 시점에 자문 항목에 넣어라.**
회피하려면 사전학습 없이 학습해야 하는데, 수백 장 규모 데이터로는 불가능하다.
